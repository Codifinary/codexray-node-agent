# Licensing

codexray-node-agent is distributed under two licenses:

| Component | Path | License |
|---|---|---|
| User-space agent (Go) | everything except `ebpftracer/ebpf/` | Apache License 2.0 |
| eBPF programs (C) | `ebpftracer/ebpf/` | GPL-2.0 |

## Why dual-licensed

The agent loads eBPF programs into the Linux kernel to perform tracing
(TCP events, file I/O, log discovery, etc.). Many of the kernel helper
functions these programs rely on are marked `GPL-only` by the kernel —
the in-kernel eBPF verifier rejects programs that use those helpers
unless the program itself declares a GPL-compatible license via:

```c
char _license[] SEC("license") = "GPL";
```

For this reason the eBPF C sources under `ebpftracer/ebpf/` are
licensed under **GPL-2.0**.

The user-space Go code that loads, manages, and reads from these
programs is licensed under **Apache License 2.0**.

## How they are distributed together

The eBPF object code is compiled separately and embedded into the Go
binary as a byte slice (see `ebpftracer/ebpf.go`, generated by
`bpf2go`). At runtime the user-space program loads this byte slice
into the kernel via the `bpf()` syscall — the GPL-2.0 program runs in
kernel space, the Apache-2.0 program runs in user space, and they
communicate via standard kernel interfaces (maps, perf buffers, ring
buffers).

This is the same arrangement used by every major Apache-2.0-licensed
eBPF tracing agent (e.g. Cilium, Pixie, Parca, Grafana Beyla,
Pyroscope), and follows the convention established by the kernel
community: GPL-licensed eBPF programs distributed alongside
differently-licensed user-space loaders do not cause the user-space
code to become a derivative work of the GPL kernel.

## Redistribution

When redistributing this software (source or binary), you must:

1. Include a copy of the Apache License 2.0 ([LICENSE](LICENSE)).
2. Include the [NOTICE](NOTICE) file, which preserves attribution to
   the upstream coroot-node-agent project.
3. Preserve the GPL-2.0 license declaration in the eBPF source files
   under `ebpftracer/ebpf/`.

If you modify Apache-licensed source files, mark them as modified per
Apache 2.0 §4(b). If you modify GPL-licensed eBPF source files, your
modifications must also be GPL-2.0.
