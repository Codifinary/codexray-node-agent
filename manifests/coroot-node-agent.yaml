apiVersion: v1
kind: Namespace
metadata:
  name: codexray

---

apiVersion: apps/v1
kind: DaemonSet
metadata:
  labels:
    app: codexray-node-agent
  name: codexray-node-agent
  namespace: codexray
spec:
  selector:
    matchLabels:
      app: codexray-node-agent
  template:
    metadata:
      labels:
        app: codexray-node-agent
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '80'
    spec:
      tolerations:
        - operator: Exists
      hostPID: true
      containers:
        - name: codexray-node-agent
          image: ghcr.io/codexray/codexray-node-agent
          args: ["--cgroupfs-root", "/host/sys/fs/cgroup"]
          ports:
            - containerPort: 80
              name: http
          securityContext:
            privileged: true
          volumeMounts:
            - mountPath: /host/sys/fs/cgroup
              name: cgroupfs
              readOnly: true
            - mountPath: /sys/kernel/debug
              name: debugfs
              readOnly: false
      volumes:
        - hostPath:
            path: /sys/fs/cgroup
          name: cgroupfs
        - hostPath:
            path: /sys/kernel/debug
          name: debugfs